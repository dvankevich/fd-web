export interface Category {
  id: string;
  name: string;
  image: string;
  description: string;
}

export interface CategoryResponse {
  _id: string;
  name: string;
  image: string;
  description: string;
}
