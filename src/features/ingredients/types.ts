export interface Ingredients {
  id: string;
  name: string;
  description: string;
  img: string;
}

export interface IngredientsResponse {
  _id: string;
  name: string;
  description: string;
  img: string;
}
